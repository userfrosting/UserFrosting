---
name:
  validators:
    length:
      label: "&NAME"
      min: 1
      max: 255
      message: VALIDATE.LENGTH_RANGE
  transformations:
  - trim
slug:
  validators:
    length:
      label: "&SLUG"
      min: 1
      max: 255
      message: VALIDATE.LENGTH_RANGE
  transformations:
  - trim
description:
permissions:
  validators:
    array:
      message: VALIDATE.ARRAY
