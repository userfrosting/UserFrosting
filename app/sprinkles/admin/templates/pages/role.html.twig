{% extends "layouts/dashboard.html.twig" %}

{% block stylesheets_page %}
    <!-- Page-group-specific CSS asset bundle -->
    {{ assets.css('css/form-widgets') | raw }}
{% endblock %}

{# Overrides blocks in head of base template #}
{% block page_title %}Roles | {{role.name}}{% endblock %}

{% block page_description %}Role information page for {{role.name}}{% endblock %}

{% block body_matter %}
    <div id="alerts-page">
    </div>
    <div class="row">
        <div class="col-lg-4">
            <div id="view-role" class="panel panel-primary">
                <div class="panel-heading">
                    <h2 class="panel-title pull-left"><i class="fa fa-fw fa-drivers-license"></i> {{role.name}}</h2>
                    <div class="clearfix"></div>
                </div>
                <div class="panel-body">
                    {% include "components/forms/role.html.twig" %}
                </div>
            </div>
        </div>
        <div class="col-lg-8">
            <div id="widget-role-permissions" class="panel panel-primary">
                <div class="panel-heading panel-heading-buttons clearfix">
                    <h3 class="panel-title pull-left"><i class="fa fa-fw fa-key"></i> {{translate('PERMISSION', 2)}}</h3>
                    <div class="pull-right">
                        <button class="btn btn-sm btn-default js-download-table">Download CSV</button>
                    </div>
                </div>
                <div class="panel-body">
                    {% include "components/tables/permissions.html.twig" with {
                            "table" : {
                                "id" : "table-role-permissions"
                            }
                        }
                    %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block scripts_page %}
    <!-- Include page variables -->
    <script>
    {% include "components/page.js.twig" %}

    // Add user name
    page = $.extend(
        true,               // deep extend
        {
            "role_slug": "{{role.slug}}"
        },
        page
    );
    </script>

    <!-- Include form widgets JS -->
    {{ assets.js('js/form-widgets') | raw }}

    <!-- Include page-specific JS -->
    {{ assets.js('js/pages/role') | raw }}
{% endblock %}
