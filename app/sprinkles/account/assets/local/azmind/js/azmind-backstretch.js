/**
 * Theme-specific Javascript file.
 */
$(document).ready(function() {

    /*
        Fullscreen background
    */
//alert(site.uri.images.background);   
    $.backstretch(site.uri.images.background);
});
